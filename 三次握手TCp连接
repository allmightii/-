第一次握手

第一次握手建立连接时候，客户端向服务器发送SYN报文（SEQ=X，SYN=1），并进入SYN_SENT状态等待服务器

第二次握手

第二次握手

  1.客户收到了客户端的请，向客户端回复一个确认信息
  2.服务器再向客户端发送一个SYN包（SEQ=y）建立连接的请求，此时服务器进入SYN——RECV状态
  
第三次握手，客户端收服务器的回复（SYN+ACk）。此时，客户端也要向服务器发送确认包（ACk）。此包发送完毕客户和服务器1进入

ESTABLISHE状态，完成三次握手

SEQ表示序列号，ACk表示确认序列号，SYN和ACK为标志位
